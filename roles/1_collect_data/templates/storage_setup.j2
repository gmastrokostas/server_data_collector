{% for item in ansible_mounts %}
{{ ansible_hostname }}|{{item.mount}}|{{item.fstype}}|{{item.device}}|{{item.options}}|{{item.uuid}}|{{ansible_date_time.date}}|{{ansible_date_time.time}}
{% endfor %}
